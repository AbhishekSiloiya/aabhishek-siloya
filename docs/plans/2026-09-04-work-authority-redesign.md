# Work authority page — implementation plan

## Scope

Local review version only. Production remains untouched.

## Implementation

1. Replace the existing hero/register/field-card structure in `review-v2/work.html` with the approved six-part casebook sequence.
2. Add contract assertions for section order, the four evidence chapters, source-backed metrics, complete organisation register and the prior-work qualifier.
3. Replace the old Work-page CSS with a scoped `.work-page` system using the current site tokens, typography, header, footer and lead dialog.
4. Add locally stored verified logo artwork where a reliable source is available; use deliberate temporary nameplates for artwork that is unavailable or ambiguous.
5. Keep TRVLR language explicitly advisory and direction-only.
6. Verify automated contracts, local links, form trigger, overflow, heading rhythm and layouts at desktop, tablet and mobile widths.

## Change log target

- Stronger opening built around consequence.
- Four fully contextualised operating chapters instead of three detached metrics.
- Complete prior-organisation register.
- Founder/operator evidence added as the bridge to current counsel.
- TRVLR reduced to a compact, qualified advisory note.
- Reduced vertical space and display-type scale.
- Mobile reading order and logo optical alignment tightened.

